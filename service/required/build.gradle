plugins 
{
	id 'java-library'
    
}

description = 'Required service support'

group = 'org.activecomponents.jadex'

if(System.getProperty("PUBLISH_SCRIPT_PATH"))
	apply from: System.getProperty("PUBLISH_SCRIPT_PATH")

sourceCompatibility = '1.17'

repositories
{
	mavenCentral()
}

dependencies
{
	implementation 'org.activecomponents.jadex:common:5.0.0'
	implementation 'org.activecomponents.jadex:bytecode:5.0.0'
	implementation 'org.activecomponents.jadex:collection:5.0.0'
	implementation 'org.activecomponents.jadex:javaparser:5.0.0'
	implementation 'org.activecomponents.jadex:core:5.0.0'
	implementation 'org.activecomponents.jadex:model:5.0.0'
	implementation 'org.activecomponents.jadex:execution:5.0.0'
	//implementation 'org.activecomponents.jadex:micro:5.0.0'
    implementation 'org.activecomponents.jadex:providedservice:5.0.0'
	implementation 'org.activecomponents.jadex:serialization:5.0.0'
	implementation 'net.cinnom:nano-cuckoo:3.0.2'
	testImplementation 'org.junit.jupiter:junit-jupiter-api:5.9.3'
}

task copyDependencies {
    doLast {
        copy {
            configurations.compileClasspath.each { File file ->
                  if (!file.absolutePath.startsWith(project.rootDir.absolutePath)) {
                    if (file.name.endsWith('.jar'))
                        from file.getParent()
                }
            } 
            into 'build/libs/external-dependencies'
        }
    }
    outputs.upToDateWhen { false }
}
build.dependsOn(copyDependencies)