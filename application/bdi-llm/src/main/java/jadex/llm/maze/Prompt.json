{
  "plan": {
    "system": "You are an expert Java code generator. Provide only the code block with a complete method implementation based on the provided goal. Don't write comments. Provide the imports if needed.",
    "user": "Create a java class named Plan which implements IPlanBody with only one method called runCode. The function receives a $Belief$ of Objects and returns a $Belief$ of Objects. At index 0 the function receives a JSONObject (org.json.simple.JSONObject). The JSONObject contains a JSONArray (org.json.simple.JSONArray) of 'OpticiansData' with different parameters. Sort the different Objects inside the JSONArray by the key 'shape'. The value of the key shape is of type string. Return the JSONObject with the sorted JSONArray 'OpticiansData' at index 0 in the $Belief$ of Objects."
  }
}